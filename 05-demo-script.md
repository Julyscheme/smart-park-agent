# 演示视频脚本 — 智慧园区工单处理 Agent（2分40秒）

> **使用方式**：用 OBS / Xbox Game Bar (Win+G) 录屏，按分镜边操作边配音（或后期配音）。建议先完整演练 2-3 遍再正式录制。
> **版本**：v2（2026-09-06 重写，对齐已发布的 12 节点工单处理工作流；v1 ChatBot 四场景旧版备份于 `%TEMP%\dify-ops\05-demo-script.v1-chatbot.bak-20260906`）

---

## 视频信息

| 项目 | 说明 |
|------|------|
| 时长 | 2分40秒（±15秒） |
| 录制工具 | OBS Studio / Xbox Game Bar（Win+G 兜底） |
| 画面 | 本地 Dify 工作流编辑器 + 日志页（1920×1080，16:9） |
| 配音 | 边操作边讲解（或先录画面后配音，剪映自动字幕） |
| 产出 | MP4 → 上传 B站/YouTube（**不公开/私密**） |

## 演示环境（已就绪，勿改动）

- 应用：**工单处理工作流 v2**（本地 Dify `http://localhost/apps` → 打开应用 → 工作流；已发布版本 2026-09-06 11:20:17）
- 模型：qwen-plus-latest ×4 节点；知识库：智谷产业园知识库（3 文档，检索阈值 0.45）
- 日志页已保留 3 条 SUCCESS 记录（勿清空，2:05 分镜要用）
- 兜底截图（现场运行失败时切换展示）：运行记录列表 / 正常工单详情 / 工单JSON输出 / 安全工单详情 / 追踪×2

---

## 分镜脚本

### 0:00-0:20 | 片头 + 画布全貌

**画面**：浏览器打开工作流编辑器，缩放至 12 节点全貌

**操作**：滚轮缩放画布，视线从「开始」节点划过两条分支到两个「结束」节点

**配音**：
> "这是基于 Dify 搭建的智慧园区工单处理 Agent——12 节点工作流。用户报修进来，经过知识库检索、信息提取、字段判断、安全检测，最后生成结构化工单。缺字段就追问，规则与LLM各司其职。"

---

### 0:20-0:55 | 场景一：正常报修 → 工单 JSON

**画面**：编辑器右上「运行」面板

**操作**：点「运行」，输入 `A座301空调不制冷，联系方式13800138000`，等待节点依次点亮，展开输出 JSON

**配音**：
> "先来一个正常报修。Agent 自动提取位置、设备类型、故障描述、联系方式四个字段，分类为空调，指派设备维修组，生成标准工单 JSON——编号、优先级、状态等十来个字段一次到位，可以直接对接物业工单系统。整个链路里 LLM 只负责理解和生成，判断逻辑全部由条件分支和代码节点完成，结果可控、可复现。人工登记一张工单要2-3分钟，这条工作流压到30秒以内。"

---

### 0:55-1:20 | 场景二：安全工单

**画面**：继续「运行」面板

**操作**：清空，输入 `B座电路跳闸，有焦糊味，疑似电路故障`

**配音**：
> "第二个场景涉及安全。代码节点做关键词安全检测，立刻命中，优先级直接拉到'高'。注意一个细节：电路故障被归为'其他'设备，但指派节点会结合故障描述把它路由到电力维修组——这是给指派 LLM 补充判定规则后实现的，避免了'其他类全部进综合事务组'的误派。"

---

### 1:20-1:45 | 场景三：缺字段追问（容错设计）

**画面**：继续「运行」面板

**操作**：输入 `空调坏了`

**配音**：
> "用户只说'空调坏了'怎么办？必要字段判断发现缺少位置和联系方式，工作流走追问分支，一句话把缺失信息问全。宁可多问一句，也不生成一张派不出去的废工单——这是容错设计。"

---

### 1:45-2:05 | 场景四：知识库检索 + 阈值调优（RAG）

**画面**：知识库页面 → 命中测试（hit-testing）；随后回编辑器运行

**操作**：
1. 知识库「命中测试」输入 `暖气不热怎么办？`，展示相似度约 0.494 的命中段落
2. 回编辑器运行 `暖气不热怎么办？`，点开「知识库查询」节点，展示命中 1 条

**配音**：
> "暖气不热这类问题，工作流第二个节点就是知识库检索。这里有一个真实的调优案例：默认相似度阈值 0.7 时，这条短查询检索不到任何内容。我用命中测试实测，相似度只有 0.494——按验收报告建议的 0.5 照样会漏。于是把阈值调到 0.45，再跑工作流，知识库节点命中数从 0 变成 1。为什么是0.45而不是建议的0.5？因为实测相似度0.494，0.5还是会漏。留点余量，数据说话。先实测、再调参、后验证——这是平台实施工作里最典型的一步。"

---

### 2:05-2:20 | 运行日志与节点追踪（工程可观测）

**画面**：左侧「日志」页，点开一条 SUCCESS 记录 → 「追踪」页签

**操作**：点开始时间为 19:24:35 的记录，切「追踪」，展示节点级耗时与 token

**配音**：
> "每一次运行都有完整日志和节点级追踪——每个节点耗时多少、消耗多少 token、输出是什么，全部可观测。对实施交付来说，这意味着出了任何问题都能定位到具体节点。"

---

### 2:20-2:40 | 价值总结（文字叠加收尾）

**画面**：切换到总结页（纯文本/PPT 均可）

**内容**：
```
一条工作流 = 提取 + 分类 + 安全检测 + 指派 + 工单生成

🔧 规则与 LLM 混合编排：判断交给分支/代码节点，生成交给 LLM
🛡️ 安全工单强制高优先级，责任部门结合故障特征智能路由
📚 RAG 知识库集成，检索阈值经实测调优（0.494 → 阈值 0.45）
🔍 全链路日志与节点级追踪，可观测、可交付

技术栈：Dify Workflow + 通义 qwen-plus-latest + RAG
可迁移：社区治理事件分派 / 客服工单路由 / 政务热线分类
```

**配音**：
> "总结一下：这套工作流把'报修登记'从人工登记的几分钟压到几十秒，规则与 LLM 各司其职。同一套编排方法可以直接迁移到社区治理事件分派、客服工单路由这些场景。完整的配置指南、测试矩阵和复现步骤都在项目仓库里，感谢观看。"

**片尾字幕**：
```
完整资料：github.com/<你的仓库地址> | Dify Agent实施 · RAG调优 · 工作流编排
系列项目：基层治理政策问答 RAG 评测（github.com/Julyscheme/gov-policy-rag）
```

---

## 录制前准备清单

- [ ] 关闭无关标签页，浏览器只留 Dify（画面干净）
- [ ] 预热：正式录制前先跑一次用例 1，确认模型响应正常
- [ ] 日志页 3 条 SUCCESS 记录保留（2:05 分镜素材）
- [ ] OBS：窗口采集（Dify 标签页），1920×1080
- [ ] 录 2-3 条备选，选最好
- [ ] 上传 B站/YouTube 设为「不公开」，链接进 README
- [ ] 可选：剪映自动字幕；补充 GitHub 仓库地址到片尾字幕

## 分镜与兜底素材对照

| 分镜 | 现场操作 | 兜底素材 |
|---|---|---|
| 场景一 | 编辑器运行 用例1 | 智慧园区Agent-S4对话效果-正常工单 / S4工单JSON输出 |
| 场景二 | 运行 用例3 | 智慧园区Agent-S4对话效果-安全工单 / S4运行追踪-安全工单 |
| 场景三 | 运行 `空调坏了` | —（该用例验收时已通过，现场直接跑） |
| 场景四 | hit-testing + 运行 `暖气不热怎么办？` | —（现场操作；失败则跳过 hit-testing 段，口述调优过程） |
| 日志追踪 | 日志页点开 19:24:35 记录 → 追踪 | 智慧园区Agent-S4运行追踪-正常工单 |
